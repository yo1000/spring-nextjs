spring:
  application:
    name: spring-nextjs

  datasource:
    url: jdbc:h2:mem:spring_nextjs

  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: "http://localhost/realms/xyz"
